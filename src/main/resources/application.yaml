spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 128MB
      max-request-size: 128MB
  profiles:
    active: dev
  mail:
    host: "smtp.gmail.com"
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
server:
  port: 8081
config:
  imagekit:
    public:
    private:
    url: https://ik.imagekit.io/minecopre
  security:
    cipher:
    signature:
  database:
    path: ./contas/
    uploadPath: ./tempFiles/